[build]
  command = "npm --prefix forex ci --no-audit --no-fund && npm --prefix forex run build"
  publish = "forex/dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
